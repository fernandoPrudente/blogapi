// import {throwUserError, messages} from './errors';
// import {expect} from 'chai';

// describe('Constants Testes - ThrowUserError Factory', () => {
//     describe('ao passar uma chave de mensagem válida, deve disparar um objeto de erro contendo a mensagem correta', () => {
//         try{
//             throwUserError(messages.PostCanNotBeNul);
//         }catch(error){
//             expect(error.message).eq()
//         }
//     });
// }); 